{
  "name": "validator",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "mocha -u bdd -R spec -t 500 --recursive",
    "test-watch": "mocha -u bdd -R spec -t 500 --recursive --watch"
  },
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^4.0.1",
    "sinon-chai": "^2.14.0"
  },
  "dependencies": {
    "sinon": "^4.0.2"
  }
}
